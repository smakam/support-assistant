# Streamlit frontend dependencies
streamlit==1.26.0
requests==2.31.0
python-dotenv==1.0.0

# Do not include uuid package - it requires compilation
# Use Python's built-in uuid module instead
# uuid==1.30

# Use precompiled Pillow instead of Pillow-SIMD which also requires compilation
Pillow==9.5.0

# For JWT handling
python-jose==3.3.0 