# Streamlit frontend dependencies
streamlit==1.26.0
requests==2.31.0
python-dotenv==1.0.0

# Do not include uuid package - it requires compilation
# Use Python's built-in uuid module instead
# uuid==1.30

# Add Pillow with binary package option
Pillow-SIMD

# For JWT handling
python-jose==3.3.0 